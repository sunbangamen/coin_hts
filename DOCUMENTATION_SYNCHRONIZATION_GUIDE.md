# Phase 3 문서 정합화 및 자동화 가이드

**작성일**: 2025-11-10
**상태**: ✅ 구현 완료
**검증 결과**: 모든 항목 통과

---

## 📋 개요

이 가이드는 Phase 3 운영 보완 프로젝트의 **문서 정합화 시스템**을 설명합니다.

### 문제점 (Before)
- 여러 문서에 중복된 상태 정보
- 수동 업데이트로 인한 수치 불일치
- 어떤 문서가 "공식" 기준인지 불명확

### 해결책 (After)
- **PHASE3_IMPLEMENTATION_STATUS.md**를 공식 기준 (소스 오브 트루스)으로 지정
- 자동화 스크립트로 다른 문서들을 동기화
- 일관성 검증 시스템 구축

---

## 🎯 소스 오브 트루스 (Single Source of Truth)

### PHASE3_IMPLEMENTATION_STATUS.md
```
🔴 이것이 공식 기준입니다.
```

이 문서가 포함하는 정보:
- ✅ 전체 테스트 통과율
- ✅ Task별 상세 구현 내역
- ✅ 성능 목표 달성 현황
- ✅ 모듈별 상태
- ✅ 실패 테스트 상세 분석
- ✅ 다음 단계 및 리스크 대응

**수정 방법**: 자동 스크립트로만 수정하세요.
```bash
python scripts/generate_phase3_status.py --input /tmp/test_results_*.json --update-docs
```

---

## 📚 보조 문서들

### PHASE3_COMPLETION_SUMMARY.md
- **용도**: 운영 가이드 (설치, 성능 튜닝, 트러블슈팅)
- **상태**: 소스 오브 트루스 자동 참조
- **수정**: 자동 스크립트 사용

### docs/coin/mvp/ri_18.md
- **용도**: 사용자 커뮤니케이션 (이슈 #29)
- **상태**: 소스 오브 트루스 링크 포함
- **수정**: 자동 스크립트 사용

### TEST_RESULTS_SUMMARY.md
- **용도**: 테스트 결과 로그
- **상태**: 별도 관리 (필요시 수동 갱신)

---

## 🔄 자동화 워크플로우

### 1단계: 테스트 실행

```bash
./scripts/run_pytest.sh
```

**동작**:
- `tests/` 전체 실행 (S3 테스트 제외)
- `/tmp/test_results_<timestamp>.json` 생성
- `/tmp/test_results_<timestamp>.log` 생성

**출력 예시**:
```
✅ 테스트 완료!
📊 결과 요약:
  - 총 테스트: 192
  - 통과: 181
  - 실패: 11
  - 통과율: 94.3%

💾 결과 저장:
  - JSON: /tmp/test_results_1731167442.json
  - Log: /tmp/test_results_1731167442.log
```

### 2단계: 문서 자동 동기화

```bash
python scripts/generate_phase3_status.py \
  --input /tmp/test_results_*.json \
  --update-docs
```

**동작**:
- 최신 JSON 파일 자동 선택 (glob 패턴)
- PHASE3_IMPLEMENTATION_STATUS.md 업데이트
- 다른 문서들의 참조 확인
- 요약 정보 출력

**출력 예시**:
```
📂 결과 파일 로드: /tmp/test_results_1731167442.json

============================================================
📊 Phase 3 상태 요약
============================================================
타임스탬프: 2025-11-10T12:30:42Z
총 테스트: 192
통과: 181 ✅
실패: 11 ❌
통과율: 94.3%
============================================================

🔄 문서 업데이트 중...
✅ PHASE3_IMPLEMENTATION_STATUS.md 업데이트 완료
   - 테스트 통과율: 181/192 (94.3%)

📝 다음 단계:
  1. git diff로 변경 사항 확인
  2. python scripts/verify_status_consistency.py로 일관성 검증
  3. git add && git commit로 커밋
```

### 3단계: 일관성 검증

```bash
python scripts/verify_status_consistency.py
```

**검증 항목**:
- ✅ 소스 오브 트루스 문서 완정성
- ✅ 보조 문서의 참조 링크
- ✅ 수치 일관성
- ✅ 메타데이터 (타임스탬프, 명령 등)

**출력 예시**:
```
📌 소스 오브 트루스 문서 검증
------------------------------------------------------------
✅ 소스 오브 트루스 표시
✅ 마지막 업데이트 시간
✅ 업데이트 명령
✅ 재현 가능 명령

결과: 4/4 통과

📚 보조 문서 검증
------------------------------------------------------------
✅ 요약 문서: 소스 오브 트루스 참조 (있음)
✅ Issue 문서: 소스 오브 트루스 참조 (있음)

============================================================
📋 Phase 3 문서 일관성 검증 결과
============================================================
✅ 모든 검증이 통과했습니다!
```

### 4단계: 커밋

```bash
git add -A
git commit -m "docs: Phase 3 상태 자동 업데이트 (181/192 통과)"
```

---

## 📊 통합 명령 (One-liner)

전체 프로세스를 한 번에 실행하려면:

```bash
./scripts/run_pytest.sh && \
  python scripts/generate_phase3_status.py --input /tmp/test_results_*.json --update-docs && \
  python scripts/verify_status_consistency.py && \
  git add -A && \
  git diff --cached --stat
```

---

## 🔒 회귀 방지 메커니즘

### CI/CD 통합 (권장)

`.github/workflows/` 또는 `.gitlab-ci.yml`에 추가:

```yaml
Phase 3 Document Sync:
  script:
    - ./scripts/run_pytest.sh
    - python scripts/generate_phase3_status.py --input /tmp/test_results_*.json --update-docs
    - python scripts/verify_status_consistency.py
  artifacts:
    paths:
      - /tmp/test_results_*.json
      - /tmp/test_results_*.log
```

### Pre-commit Hook (선택)

`.git/hooks/pre-commit` 에 추가:

```bash
#!/bin/bash
if git diff --cached | grep -q "PHASE3_IMPLEMENTATION_STATUS.md"; then
  echo "🔍 문서 일관성 검증 중..."
  python scripts/verify_status_consistency.py || exit 1
fi
```

---

## 📝 각 문서의 역할

| 문서 | 역할 | 수정 방식 | 대상자 |
|------|------|---------|-------|
| **PHASE3_IMPLEMENTATION_STATUS.md** | 공식 기준 (SOT) | 자동 스크립트만 | 개발팀, CI/CD |
| **PHASE3_COMPLETION_SUMMARY.md** | 운영 가이드 | 자동 참조 | 운영팀, 신규 온보딩 |
| **ri_18.md** | 사용자 커뮤니케이션 | 자동 참조 | 외부 이해관계자 |
| **TEST_RESULTS_SUMMARY.md** | 테스트 로그 | 필요시 수동 | 품질보증팀 |

---

## 🛠️ 스크립트 상세 설명

### run_pytest.sh

```bash
./scripts/run_pytest.sh
```

**기능**:
- pytest 전체 실행 (S3 테스트 제외)
- JSON 포맷의 결과 저장
- 로그 파일 생성
- 통과율 계산

**환경 변수**:
- `PYTHONPATH=.` (자동 설정)
- 가상환경 자동 활성화

**출력 파일**:
- `/tmp/test_results_<timestamp>.json`: 구조화된 결과
- `/tmp/test_results_<timestamp>.log`: 상세 로그

### generate_phase3_status.py

```bash
python scripts/generate_phase3_status.py \
  --input /tmp/test_results_*.json \
  --update-docs
```

**인자**:
- `--input`: pytest JSON 결과 파일 (glob 지원)
- `--update-docs`: 문서 자동 업데이트 플래그 (기본: False)

**동작**:
1. JSON 파일 로드 및 파싱
2. 상태 메트릭 추출
3. PHASE3_IMPLEMENTATION_STATUS.md 업데이트
4. 요약 정보 출력

**에러 처리**:
- 파일 미존재 시 명확한 메시지
- 잘못된 JSON 형식 시 예외 처리

### verify_status_consistency.py

```bash
python scripts/verify_status_consistency.py
```

**검증 내용**:
1. **소스 오브 트루스 검증** (4항목)
   - 문서 마크 확인
   - 메타데이터 존재 확인
   - 업데이트 명령 기재 확인
   - 재현 명령 기재 확인

2. **보조 문서 검증** (2항목)
   - 요약 문서 참조 확인
   - Issue 문서 참조 확인

3. **수치 일관성 검증**
   - 메트릭 패턴 추출
   - Task 상태 추출

**종료 코드**:
- `0`: 성공 (또는 경고만 있음)
- `1`: 에러 발생

---

## 🚀 추천 사용 시나리오

### 시나리오 1: 주간 상태 리포트
```bash
# 매주 금요일 자동 실행 (CI/CD 스케줄)
./scripts/run_pytest.sh
python scripts/generate_phase3_status.py --input /tmp/test_results_*.json --update-docs
git add -A && git commit -m "Weekly Phase 3 status update"
git push origin feature/phase3
```

### 시나리오 2: 로컬 개발 중 검증
```bash
# 코드 변경 후 테스트 및 문서 검증
./scripts/run_pytest.sh

# 실패가 있으면 수정 후 다시 실행
./scripts/verify_status_consistency.py

# 모두 통과하면 커밋
git add -A && git commit
```

### 시나리오 3: PR 리뷰
```bash
# PR 메인테이너가 실행
./scripts/run_pytest.sh
python scripts/generate_phase3_status.py --input /tmp/test_results_*.json --update-docs
python scripts/verify_status_consistency.py

# 결과가 PR 코멘트에 추가됨
```

---

## ⚠️ 주의사항

### ❌ 하지 말아야 할 것
- ❌ PHASE3_IMPLEMENTATION_STATUS.md를 직접 편집
- ❌ 수치를 수동으로 변경
- ❌ 문서를 다르게 수정해서 버전 불일치 생성

### ✅ 해야 할 것
- ✅ 스크립트를 통해 자동 업데이트
- ✅ `verify_status_consistency.py`로 정기 검증
- ✅ 스크립트 자체는 git에 커밋

---

## 📞 문제해결

### Q: 스크립트가 실행되지 않음
```bash
# 실행 권한 확인
ls -la scripts/run_pytest.sh

# 권한 부여
chmod +x scripts/run_pytest.sh
```

### Q: JSON 파일을 찾을 수 없음
```bash
# 최신 파일 확인
ls -lt /tmp/test_results_*.json | head -3

# 명시적으로 파일 지정
python scripts/generate_phase3_status.py --input /tmp/test_results_1731167442.json
```

### Q: 문서 업데이트가 안 됨
```bash
# 업데이트 플래그 확인
python scripts/generate_phase3_status.py --input /tmp/test_results_*.json --update-docs

# git 상태 확인
git status
```

---

## 📚 관련 문서

- [PHASE3_IMPLEMENTATION_STATUS.md](./PHASE3_IMPLEMENTATION_STATUS.md) - 소스 오브 트루스
- [PHASE3_COMPLETION_SUMMARY.md](./PHASE3_COMPLETION_SUMMARY.md) - 운영 가이드
- [Issue #29](./docs/coin/mvp/ri_18.md) - 사용자 문서

---

**마지막 업데이트**: 2025-11-10
**검증 상태**: ✅ 모든 스크립트 작동 확인
